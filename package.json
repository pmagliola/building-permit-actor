{
  "name": "us-building-permit-scraper",
  "version": "0.1.16",
  "description": "Apify Actor — scrape US building permits from city open data portals for construction lead generation",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "start": "node dist/main.js",
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "apify": "^3.2.6"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
